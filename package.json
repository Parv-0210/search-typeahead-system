{
  "name": "search-typeahead-system",
  "version": "1.0.0",
  "description": "A search typeahead system with prefix suggestions, distributed cache (consistent hashing), recency-aware trending, and batched writes.",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "generate": "node scripts/generate-dataset.js",
    "benchmark": "node scripts/benchmark.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "license": "MIT"
}
